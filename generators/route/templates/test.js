var request = require('supertest-koa-agent');
var code = require('api-code');
var bootstrap = require('../bootstrap')
var app = require('../../app');

describe('<%= name %> route', () => {

    describe('POST /api/<%= name %>', () => {

    })

    describe('PUT /api/<%= name %>/:<%= name %>', () => {

    })

    describe('PATCH /api/<%= name %>/:<%= name %>', () => {

    })

    describe('GET /api/<%= name %>', () => {

    })

    describe('GET /api/<%= name %>/:<%= name %>', () => {

    })

    describe('DELETE /api/<%= name %>/:<%= name %>', () => {

    })
})
